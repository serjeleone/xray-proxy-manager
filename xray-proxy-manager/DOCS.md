# Xray Proxy Manager — техническая документация

## 1. Назначение

Xray Proxy Manager загружает подписку в формате Xray JSON — готовый набор конфигураций с одним или несколькими outbound, правилами маршрутизации и сопутствующими параметрами. Приложение разбирает такую структуру, выделяет доступные outbound, формирует из них управляемые конфигурации Xray и предоставляет рабочие SOCKS5-слоты с проверкой и переключением. Поддерживаются два режима:

- **однослотовый** — используется только один слот, `xray-a`, а переключение выполняется через перезапуск процесса с разрывом существующих соединений.
- **двухслотовый** — `xray-a` и `xray-b` работают как blue-green пара;

Менеджер предоставляет сетевые точки подключения к Xray и может координировать их с совместимым внешним selector. Клиентский трафик может поступать к слотам напрямую либо через внешний прокси, маршрутизатор или сетевой шлюз. Прозрачная маршрутизация, маркировка пакетов, правила обхода и публикация selector API задаются внешней сетевой конфигурацией.

## 2. Архитектура и режимы

| Слот | Порт по умолчанию | Конфигурация |
|---|---:|---|
| `xray-a` | `10808` | `/config/config.xray-a.json` |
| `xray-b` | `10809` | `/config/config.xray-b.json` |

### Однослотовый режим

```text
создание и xray -test новой конфигурации без остановки текущего процесса
→ остановка xray-a и всех его соединений
→ запуск и проверка xray-a
→ продолжение работы через тот же SOCKS-порт
```

Параллельного рабочего Xray-процесса нет, поэтому происходит кратковременный обрыв. Новая конфигурация формируется и проверяется заранее, пока старый процесс ещё обслуживает трафик.

### Двухслотовый режим

```text
подписка → свободный слот → xray -test → проверка двух URL
          → переключение внешнего selector
          → новый активный слот + дренирование старого
```

Новый outbound заранее запускается в свободном слоте. После переключения selector новые подключения направляются в новый слот. Старый Xray продолжает обслуживать уже установленные соединения до естественного завершения, ручной остановки, аварийного failover или таймаута.

Открытое TCP/UDP-соединение нельзя перенести между процессами или конфигурациями Xray. После разрыва клиент должен создать новое соединение.

## 3. Управление внешним selector и подключение трафика

При `selector_control_enabled: true` приложение обращается к внешнему `selector_api_url` и использует `selector_api_secret` как Bearer-токен, если секрет задан.

Совместимый selector должен содержать точные элементы:

```text
xray-a
xray-b
```

Для автоматического переключения API должно обеспечивать:

- чтение текущего элемента selector;
- выбор `xray-a` или `xray-b`.

Для полной диагностики дренирования желательно также получать список соединений и цепочки использованных outbound.

Если selector API недоступно, новое управляемое двухслотовое переключение не начинается. Уже работающий Xray-процесс продолжает обслуживать трафик.

Сохранённый `active_slot_tag` менеджера является ожидаемым активным слотом. Значение, прочитанное из внешнего selector, не заменяет это состояние, пока ожидаемый Xray-процесс работает.

Пример:

```text
менеджер ожидает xray-b
selector сообщает xray-a
xray-b работает
→ менеджер немедленно записывает xray-b обратно в selector
```

Менеджер принимает значение selector только при одновременном выполнении двух условий:

1. ожидаемый менеджером слот действительно остановлен;
2. слот, сообщённый selector, запущен.

Опрос динамический:

- в обычном состоянии — `selector_status_interval_seconds`, по умолчанию 10 секунд;
- пока слот дренируется — раз в 1 секунду;
- после недоступности API — раз в 1 секунду до восстановления;
- после восстановления API ожидаемый слот принудительно записывается в selector и проверяется повторным GET-запросом.

### Входящая скорость

Бейдж обновляется раз в секунду и показывает скорость загрузки выбранного слота в МБ/с (1 МБ = 1 000 000 байт), с одним знаком после запятой. Исходящая скорость и трафик другого, в том числе дренируемого, слота не суммируются. При переключении слота или источника статистики первый замер задаёт новую точку отсчёта.

При `selector_control_enabled: true` используются поля `download` соединений из `/connections`, в чьём `chains` есть выбранный слот. Это сохраняет живые показания при длительных TCP-загрузках, включая VLESS Vision. Бейдж в этом режиме учитывает трафик, проходящий через selector.

Без selector используется локальный Xray StatsService: разница накопительного `inbound>>>socks>>>traffic>>>downlink` за прошедшее время, без сброса счётчика. В Xray v26.9.9 при Linux TCP splice ядро обновляет inbound-, outbound- и user-счётчики только после завершения копирования соединения. Поэтому один StatsService не обеспечивает живую скорость таких потоков; замена inbound-счётчика на outbound это ограничение не устраняет. Если источник статистики недоступен, UI показывает «— МБ/с» с причиной в подсказке.

### График переключений

Нажатие на название активного outbound открывает график за последние 12 часов; повторное нажатие или Escape закрывает его. Круговая анимация заменяет только текстовую область верхней карточки. При включённом уменьшении движения переход выполняется без анимации.

По оси Y показано количество завершённых смен outbound за 5 минут, по оси X — время с отметками через 3 часа. Текущий неполный интервал обновляется вместе со статусом каждые 3 секунды, поэтому новые переключения видны без ожидания конца пятиминутки. Наведение или касание графика показывает время интервала и точное количество.

История хранится в `/config/switch-history.json` независимо от ограничения лога в 2500 строк. Данные записываются при тех же успешных сменах и откатах, которые отражаются в журнале, и загружаются в память при запуске. Неудачные попытки, первый запуск и перезапуск того же outbound не учитываются. События старше 12 часов удаляются; несколько переключений в одну секунду сохраняются общим счётчиком. После первого обновления до версии с графиком история начинает накапливаться заново, затем сохраняется между перезапусками.

## 4. Двухслотовое переключение

Обычная операция выполняется в следующем порядке:

1. Менеджер проверяет, что selector соответствует ожидаемому активному слоту.
2. Свободный слот запускается с новой конфигурацией.
3. Выполняются `xray -test` и параллельные запросы к `primary_test_url` и `secondary_test_url`.
4. Selector переключается на новый слот и проверяется чтением текущего значения.
5. Новое состояние записывается в `state.json`.
6. Старый слот переводится в draining и контролируется до остановки.
7. В первые 30 секунд новый слот проверяется повторно; при двух ошибках выполняется rollback.

## 5. Дренирование и диагностика соединений

После ручного переключения дренируемый слот остаётся под активным с жёлтой рамкой и жёлтой заливкой пинга, независимо от фильтров и сортировки. После завершения дренирования или нажатия кнопки «Стоп» outbound переносится в конец списка доступных. Жёлтая заливка пинга сохраняется до обновления подписки или повторного выбора.

Дренируемый слот останавливается штатно, когда:

```text
tracked_connections = 0
AND traffic_counters_are_stable
AND drain_quiet_seconds elapsed
AND post-switch protection elapsed
```

Менеджер сопоставляет соединения selector API с `chains`, дополнительно считает живые TCP-сокеты локального SOCKS-порта и выводит на уровне info:

- общее число соединений;
- TCP и UDP отдельно;
- объём трафика;
- число соединений без изменения счётчиков не менее 10 секунд;
- число подключений, появившихся уже после перевода слота в draining.

Сводка пишется при изменении количества соединений и не реже одного раза в 30 секунд. При `log_level: debug` добавляются примеры соединений с ID, исходным адресом, протоколом, назначением, цепочкой outbound и счётчиками байтов.

Новое соединение, обнаруженное в `chains` дренируемого слота, фиксируется отдельным предупреждением. Это позволяет отличить ошибку selector от старой сессии, которая законно продолжает жить после переключения.

`drain_timeout_minutes: 0` оставляет ожидание неограниченным. Кнопка **«Стоп»** немедленно завершает дренируемый процесс. Недоступный дренируемый слот останавливается после `auto_check_failures` неудачных проверок подряд независимо от числа соединений.

## 6. Проверки outbound

Ручное переключение доступно как во время проверки слотов, так и во время общей проверки outbound. Результаты ранее запущенных проверок не отменяют ручной выбор. Повторное переключение возможно после завершения текущего.

Во всех проверках оба адреса запрашиваются параллельно:

- `primary_test_url`;
- `secondary_test_url`.

Если отвечает хотя бы один адрес, проверка доступности считается успешной и используется меньшая успешная задержка. Если меньшая задержка превышает `auto_check_max_latency_ms`, результат считается деградацией. Ошибка доступности фиксируется только при недоступности обоих адресов.

Проверка активного слота выполняется каждые `auto_check_interval_seconds` и пишется в info-лог вместе с результатами обоих адресов, лучшей задержкой и текущим счётчиком ошибок. Полная проверка всех outbound выполняется отдельно каждые `auto_best_check_interval_seconds`.

После обновления подписки автоматически запускается полная проверка. Интервал до следующей полной проверки отсчитывается от её завершения.

При включённых `auto_checker_enabled` и `auto_switch_best_enabled` после `auto_check_failures` последовательных ошибок активного слота запускается аварийный failover. Отключение автопереключения отменяет также выбор по результатам уже запущенной проверки и при изменении предпочтений; проверки могут продолжаться без смены outbound. После двух успешных проверок нового пути старый деградировавший слот принудительно останавливается, чтобы клиенты создали новые соединения.

## 7. Обновление подписки

Подписка обновляется напрямую. Если прямое подключение не удалось, выполняется резервная попытка через уже работающие Xray-слоты: сначала через активный, затем через второй запущенный слот, если он доступен.

Загрузка подписки не блокирует ручной выбор. Если нажатие совпало с применением загруженного списка, выбор выполняется после его применения и использует актуальную конфигурацию outbound.

После успешной загрузки обновляются сохранённая подписка и список outbound. Изменённая конфигурация активного outbound будет применена кнопкой «Применить» в его карточке или при смене режима слотов.

При ошибке используется последняя сохранённая подписка. В UI появляется **«Ошибка синхронизации»** и текст ошибки. Если загруженные данные нельзя разобрать или применить, отображается сообщение: **«Загруженную подписку не удалось применить; предыдущая рабочая подписка сохранена.»**

## 8. Параметры конфигурации

### Подписка и базовые параметры

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `subscription_url` | пусто | HTTP(S)-адрес JSON-subscription. |
| `config_index` | `0` | Резервный индекс корневой конфигурации подписки. |
| `update_interval_hours` | `1` | Период обновления; `0` отключает расписание. |
| `dual_slot_enabled` | `true` | `true` — blue-green A/B; `false` — только `xray-a` с разрывом соединений при переключении. |
| `log_level` | `warning` | Уровень журнала Xray: `debug`, `info`, `warning`, `error`, `none`. Диагностика менеджера уровня debug также включается этим значением. |
| `user_agent` | `XPM-HA-App` | User-Agent загрузчика подписки. |

### SOCKS5

Для каждого слота задаётся общий TCP/UDP-порт. Приложение использует сеть узла и не публикует отдельные mappings `ports`.

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `socks_listen_address` | `0.0.0.0` | Адрес прослушивания SOCKS5; `127.0.0.1` разрешает подключения только с узла Home Assistant. |
| `socks_allowed_cidrs` | `["0.0.0.0/0"]` | Разрешённые исходные сети IPv4/IPv6. Значение `router` этого параметра позволит получить LAN-подсети от OpenWrt через SSH при запуске. |
| `socks_tcp_a` | `10808` | Единый TCP/UDP-порт SOCKS5 `xray-a`. |
| `socks_tcp_b` | `10809` | Единый TCP/UDP-порт SOCKS5 `xray-b`; должен отличаться от A. |
| `proxy_username` | пусто | Имя пользователя SOCKS5; указывается вместе с паролем. Опционально. |
| `proxy_password` | пусто | Пароль SOCKS5; указывается вместе с именем. Опционально. |

По умолчанию сохраняется доступ с роутера и напрямую из локальной сети. Для ограничения своей LAN задайте, например, `socks_allowed_cidrs: ["192.168.1.0/24"]` либо `["router"]` при настроенном SSH-доступе к OpenWrt (`router_*`). Учитывается адрес источника, который видит Xray: для дополнительных маршрутизируемых сетей укажите их CIDR отдельно. Проверки менеджера через loopback разрешены всегда. Настройки адреса и сетей применяются после перезапуска приложения.

### Проверки и автоматический выбор

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `auto_checker_enabled` | `true` | Включить периодические проверки. |
| `auto_switch_best_enabled` | `true` | После полной проверки выбирать достаточно более быстрый outbound. |
| `switching_preset` | `smooth` | `smooth` — штатное дренирование; `adaptive` — закрытие зависших сессий и остановка через 30 с; `forced` — немедленное закрытие старых сессий. |
| `auto_switch_preferred_country` | пусто | ISO-код страны с приоритетом; при отсутствии подходящих серверов используется общий список. |
| `auto_switch_preferred_protocol` | пусто | Протокол с приоритетом; значения в UI формируются из подписки. |
| `auto_switch_excluded` | `RU` | ISO-коды стран и текстовые фрагменты через запятую, исключаемые из автоматического выбора. |
| `auto_switch_min_ping_delta_ms` | `100` | Минимальное улучшение для обычного автопереключения. |
| `auto_check_interval_seconds` | `60` | Период проверки текущего активного слота. |
| `auto_best_check_interval_seconds` | `600` | Период полной проверки списка. |
| `auto_check_max_latency_ms` | `500` | Максимальная задержка, при которой сервер допускается к выбору; `0` — без ограничений. |
| `auto_check_failures` | `3` | Порог последовательных ошибок для failover и остановки недоступного дренируемого слота. |
| `auto_check_timeout_seconds` | `12` | Таймаут каждого URL при проверке активного слота. |
| `latency_test_timeout_seconds` | `12` | Таймаут каждого URL при полной/ручной проверке. |
| `latency_test_parallelism` | `0` | `-1` — все outbound одновременно; `0` — автоматический предел от 2 до 8; `1–32` — заданный максимум. |
| `primary_test_url` | `https://www.gstatic.com/generate_204` | Первый контрольный URL. |
| `secondary_test_url` | `https://cp.cloudflare.com/generate_204` | Второй независимый контрольный URL. |

### Веб-интерфейс

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `ui_port` | `8090` | Порт внутреннего веб-интерфейса; изменение применяется после перезапуска приложения. |
| `ui_sort` | `ping-asc` | Сортировка: имя, задержка или протокол в обоих направлениях сортировки. |
| `ui_protocol_filter` | `all` | Фильтр протокола. |
| `ui_max_ping_ms` | `1000` | Максимальная отображаемая задержка; `0` отключает предел отображения. |
| `ui_hide_unavailable` | `false` | Скрывать недоступные outbound. |
| `ui_hide_excluded` | `true` | Скрывать совпавшие с `auto_switch_excluded` outbound. |

При работе через Home Assistant Ingress Supervisor автоматически выделяет свободный внешний порт и перенаправляет запросы на `ui_port`. Watchdog использует отдельный служебный health endpoint, поэтому изменение `ui_port` не нарушает контроль состояния приложения.

### External selector control и дренирование

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `selector_control_enabled` | `false` | Управлять совместимым внешним selector через API. Для автоматического двухслотового переключения менеджером должно быть включено. |
| `selector_api_url` | `http://192.168.0.1:9090` | Базовый URL совместимого selector API. |
| `selector_api_secret` | пусто | Bearer-токен selector API. |
| `selector_tag` | `xray-active` | Тег selector, содержащего `xray-a` и `xray-b`. |
| `selector_status_interval_seconds` | `10` | Обычный интервал опроса; при дренировании/ошибке API автоматически становится 1 секундой. |
| `drain_quiet_seconds` | `30` | Период нулевой активности до штатной остановки. |
| `drain_poll_interval_seconds` | `2` | Период анализа соединений дренируемого слота. |
| `drain_timeout_minutes` | `0` | Предельное время дренирования; `0` — без таймаута. |

### OpenWrt

| Параметр | По умолчанию | Назначение |
|---|---:|---|
| `router_control_enabled` | `true` | Включить SSH-управление UCI-правилом на OpenWrt. |
| `router_host` | `192.168.0.1` | Адрес хоста OpenWrt. |
| `router_ssh_port` | `22` | SSH-порт. |
| `router_ssh_user` | `root` | SSH-пользователь. |
| `router_auth_method` | `existing_key` | `existing_key`, `generate_key` или `password`. |
| `router_ssh_key_name` | `id_ed25519` | Имя приватного ключа в каталоге `ssh`; без пути. |
| `router_ssh_key_path` | пусто | Необязательный абсолютный путь к приватному SSH-ключу внутри контейнера приложения. Например `/config/ssh/id_ed25519`. |
| `router_ssh_password` | пусто | Пароль для подключения при выборе `password` или однократной установки сгенерированного SSH-ключа на роутер. |
| `router_firewall_rule` | `mark_domains` | Имя правила маршрутизации. |
| `router_status_interval_seconds` | `10` | Период проверки правила. |

## 9. Веб-интерфейс

Верхний блок содержит:

- имя активного outbound;
- IP-адрес и порт подключения активного слота, а также режим слотов;
- состояние дренируемого слота и число соединений;
- кнопки проверки, смены одно-/двухслотового режима и принудительной остановки текущего дренируемого слота.

Карточки outbound показывают индикаторы `A` и `B` для работающих слотов. Активный outbound закрепляется первым. После успешного ручного переключения прежний outbound получает жёлтый пинг и размещается под зелёными; выбранная сортировка применяется внутри каждой группы. Отметка сохраняется до успешного обновления подписки. Остальные дренируемые outbound закрепляются под активным. Если ручной выбор не прошёл проверку, outbound становится «недоступен» и прежний пинг удаляется. Ручное переключение доступно во время общей проверки; её устаревший результат не отменяет ручной выбор. Окно логов поддерживает поиск, перенос строк, переход в начало и конец, копирование и выгрузку журнала в файл.

Через кнопку `Экспорт` в блоке с подпиской можно выгрузить текущую конфигурацию в sing-box-формате при необходимости её использования в соответствующем стороннем клиенте.

## 10. Хранение данных

| Путь | Назначение |
|---|---|
| `/config/subscription.json` | Последняя успешно загруженная подписка. |
| `/config/config.xray-a.json` | Конфигурация `xray-a`. |
| `/config/config.xray-b.json` | Конфигурация `xray-b`. |
| `/config/config.last_good.json` | Последняя рабочая конфигурация. |
| `/config/config.last_good.meta.json` | Метаданные последней рабочей конфигурации. |
| `/config/state.json` | Активный outbound/слот, задания и ошибки. |
| `/config/latencies.json` | Результаты проверок. |
| `/config/runtime-options.json` | Настройки, изменённые из UI. |
| `/config/ssh/` | SSH-ключи удалённого хоста. |

## 11. Версия

Текущая версия указана в [VERSION](VERSION).

Релиз проходит проверки Python, браузерного интерфейса и передачи трафика через настоящий Xray, затем собираются образы для amd64 и aarch64. Ветки `release/**` запускают проверки без публикации.

Для пересборки существующего тега в сообщении коммита, переносимого в `main`, указывается отдельный завершающий блок `Rebuild-Release: vX.Y.Z`, совпадающий с `VERSION`. Также доступен ручной запуск workflow с параметром `publish: true`. После успешной сборки обновляются образы, тег и описание релиза. Ветка `release/vX.Y.Z` удаляется только при полном совпадении с опубликованным коммитом `main`.
